/**
 * SessionRunner was removed in Phase 3 — superseded by SessionServiceImpl.
 * This file is retained as a stub to avoid git history churn.
 * Session orchestration tests now live in session-service.test.ts.
 */

import { describe, it } from "vitest";

describe("SessionRunner (removed)", () => {
  it("SessionRunner is no longer exported — see SessionServiceImpl", () => {
    // Intentionally empty. Multi-turn orchestration is now in SessionServiceImpl.
  });
});
